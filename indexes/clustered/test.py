# TODO: only need to implement on top of Parquet file system since the implementations will differ depending on usage
# Algorithm - sorting initial data according sort key (col1, col2, col3 .....) with sort() function, and then binary search with bisect() module
# It is single dimension index, so obviously it will perform the worse
